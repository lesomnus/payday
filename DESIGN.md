# payday

gRPC 애플리케이션 프레임워크. 무엇을 만들 것인지, 무엇을 물려받고 무엇을 새로 정할
것인지, 무엇을 강제할 것인지에 대한 최초 검토.

## 0. 무엇을 만드는가

`lesomnus/go-app`은 잘 만들어진 **템플릿**이다. 복사해서 시작하고, 복사한 뒤에는 각자의
것이 된다. 프레임워크를 의도하고 만든 것이 아니므로 그 구조가 그대로 프레임워크의
구조인 것은 아니다. 이 문서에서 go-app은 **선행 사례**로 다룬다 — 어떤 결정이 앱을
하나 만들어 보니 옳았는가에 대한 증거이지, 따라야 할 설계가 아니다.

payday가 하려는 것은 그 템플릿에서 **앱마다 달라지지 않는 부분을 떼어 모듈로 만들고,
달라지는 부분에는 형태를 강제하는 것**이다. 복사가 아니라 의존이 되면 한쪽에서 고친
버그가 다른 쪽에 남지 않는다.

네 가지를 낸다.

| | 무엇 | 형태 |
| --- | --- | --- |
| **스키마** | Tenant, Holder, Audit — 모든 앱이 갖는 엔티티 | payday의 베이스 proto |
| **레이어** | `gate`(누가 무엇을 볼 수 있나)와 `audit`(무엇이 바뀌었나) | 앱에 생성, 로직은 런타임 |
| **런타임** | 앱마다 똑같이 쓰이는 Go 코드 | `github.com/lesomnus/payday` 모듈 |
| **CLI** | 코드 생성, 스캐폴딩, 검사 | `go tool pd` |
| **템플릿** | 배선과 결정이 보이는 시작점 | CLI가 찍어냄 |

앱이 손으로 쓰는 서버 레이어는 **`core` 하나**로 줄어든다 — 자기 도메인 규칙과,
CRUD가 아니어서 생성되지 않는 RPC들.

### 이름

CLI는 `pd`다. 자주 치는 명령이고, `go tool pd`로 부르므로 PATH에 올라가지 않는다 —
같은 이름의 다른 도구(PagerDuty CLI가 `pd`다)와 부딪힐 자리가 없다.

Go 패키지에 `pd`를 붙일지는 **하나의 기준**으로 정한다.

> **payday가 *정의한* 형식인가, 아니면 일반적인 개념인가.**

| 패키지 | 이름 | 왜 |
| --- | --- | --- |
| ID | `pdid` | payday가 정의한 형식이다. 그리고 `id`는 **지역 변수로 상시 가려진다** — `id, err := ...` 한 줄이면 그 스코프에서 패키지가 사라진다 |
| slug | `slug` | payday가 정한 형식이긴 하지만 `id`처럼 가려지는 이름이 아니고, 부르는 곳도 많지 않다. 접두사가 값을 못 낸다 |
| 테스트 하네스 | `pdtest` | `httptest`/`iotest`의 관례를 따른다 |
| `auth`, `frame`, `gate`, `audit`, `config`, `migrate`, `version`, `spin` | 그대로 | 전부 일반 개념이다. 접두사는 정보를 더하지 않고 길이만 더한다 |
| `grpcx` | 그대로 | `x` 접미사가 이미 관례다 |
| 생성된 proto 메시지 | `pdpb` | 같은 접두사. `paydaypb`는 길기만 하다 |

**루트 패키지는 두지 않는다.** API는 전부 하위 패키지에 산다 — 그러면
`github.com/lesomnus/payday/pdid` → `package pdid`처럼 import 경로의 마지막 조각과
패키지 이름이 어디서도 어긋나지 않는다. 루트에 둘 만한 것(`Domain`, `Register`,
`Minter`)은 어차피 전부 `pdid`의 것이다.

**proto 패키지는 `payday`로 둔다.** proto 패키지 이름은 전역 이름공간이고, 거기서
`pd`는 너무 흔하다. 그리고 자주 치는 이름도 아니다 — 엔티티마다 한 번씩
`option (payday.entity)`로 나올 뿐이다.

타입 이름은 `pdid.Id`로 적었다. Go 관례로는 `ID`가 맞지만 `hid.Id`와 나란히 읽히는
편이 나을 것 같아서인데, 패키지가 이미 접두사를 지고 있으니 어느 쪽이든 상관없다.

## 1. 물려받는 겹, 그리고 무엇을 건드리는가

프레임워크의 대부분은 이미 존재한다.

| 겹 | 무엇 | 어디에 | payday가 |
| --- | --- | --- | --- |
| 계약 | `orm.field`, `orm.message` | `protobuf-orm` | **건드리지 않는다** |
| 생성기 | 서비스 계약, 메시지, ent 스키마, CRUD 서버 | `protoc-gen-orm-{go,ent,service}` | 훅 하나 추가 ([SCHEMA.md](docs/SCHEMA.md)) |
| 런타임(생성물용) | `enttx`, `entpatch`, `entpage` | `protoc-gen-orm-ent/runtime` | 그대로 씀 |
| 런타임(횡단) | `otx`, `xli`, `mkot`, `z` | 각 저장소 | 그대로 씀 |
| **스키마 + 런타임(앱)** | Tenant/Holder/Audit, `grpcx`, `config`, `frame`, `auth`, `pdid` | **비어 있음** | **payday** |

`protobuf-orm`을 건드리지 않는 것은 실무적으로 큰 차이다. 그 스키마는 원격 레지스트리
(`buf.build/orm/orm`)에 게시되고 `buf.lock`으로 고정되므로, 거기에 필드를 하나 넣으려면
**게시를 기다려야 한다** — go-app이 soft delete에서 실제로 멈춰 섰던 지점이다. payday가
자기 확장을 자기 proto에 정의하면 그 리듬에서 벗어난다.

`protoc-gen-orm-ent`는 payday의 필요에 따라 고친다. 다만 **고치는 내용이 payday를 알지
않도록** 한다([SCHEMA.md](docs/SCHEMA.md)) — 그러면 포크가 아니라 상류 변경이 되고, 유지할 포크가 하나 줄어든다.


## 2. 문서 지도

| | 무엇 |
| --- | --- |
| **[docs/SCHEMA.md](docs/SCHEMA.md)** | payday가 소유하는 스키마(Tenant/Holder/Audit), 참조(ID와 slug), 선언에서 생성되는 `List`/`Watch` |
| **[docs/RUNTIME.md](docs/RUNTIME.md)** | 런타임 패키지 구성, CLI, **강제할 것**, 레이어 라이프사이클 |
| **[docs/CLIENT.md](docs/CLIENT.md)** | 브라우저 안의 서버(샌드박스), UI 층 나누기, 로컬 스토어 |
| **[docs/PLAN.md](docs/PLAN.md)** | 저장소 배치, 아직 답하지 않은 것, 작업 순서 |

## 3. 확인된 전제

계획이 서 있는 가정 중 검증한 것. **셋 다 통과했고, 셋 다 조건이나 결과가 딸려 나왔다.**

**패키지를 넘는 엣지 — 된다.** `base`/`app` 두 패키지로 최소 예제를 만들어
`buf generate` → `ent generate` → `go build`까지 통과. 조건 둘: 플러그인은 `strategy: all`로
불러야 하고(기본 `directory`는 디렉터리마다 호출해서 참조 대상이 생성 대상이 아니게 된다),
**한 앱의 모든 엔티티가 하나의 `go_package`를 공유해야 한다**(다르면 ent 스키마가 갈라져
엣지가 컴파일되지 않는다). 자세히는 [SCHEMA.md](docs/SCHEMA.md).

**drpc가 gRPC와 같은 인터셉터 타입을 쓴다 — 어댑터가 필요 없다.**
`grpc.UnaryServerInterceptor`, `grpc.StreamServerInterceptor`, `stats.Handler`를 그대로
받고 `var _ grpc.ServiceRegistrar = &Server{}`다. 그러니 브라우저 샌드박스와 배치가
요구하는 리팩터링은 하나뿐 — **인터셉터를 옵션 슬라이스가 아니라 맨 함수로 내보내는 것.**
두 기능이 같은 것을 원하므로 비용은 한 번만 치른다.

**오버레이 병합이 엔티티 메시지에도 된다 — 그리고 조용히 덮는다.** `protobuf-merge`가
`Holder`에 필드를 더하면서 `orm` 옵션도 `reserved`도 보존한다. 다만 오버레이가 payday
소유 번호를 다시 선언하면 **경고 없이 이긴다** — `string alias = 4`가 `int64 alias = 4`가
된다. [RUNTIME.md](docs/RUNTIME.md)의 강제 항목 2b는 종이 위 걱정이 아니었다.

## 4. 프로토타입까지 — 어디서 끊고 무엇을 보는가

계획은 프로토타입까지 갈 수 있다. 다만 **단계가 아니라 체크포인트로 끊는다** — 각
지점은 "여기까지 만들었다"가 아니라 **"여기서 멈추고 무언가를 배운다"** 여야 한다.
배울 것이 없는 지점은 끊을 이유가 없고, 되돌리기 비싼 결정 앞은 반드시 끊는다.

[PLAN.md](docs/PLAN.md)의 번호가 작업이고, 아래가 그것을 묶는 마디다.

### CP0 — 전제 · **완료**

§3. 셋 다 확인됨.

### CP1 — 선언에서 강제가 나오는가 · **완료**

`pdid` + `payday.entity` 옵션 + `protoc-gen-pd` + `Minter` 상류 변경 + 선언에서
벽 생성. 시험 대상은 `internal/apptest` — 테넌트 하나, 테넌트에 속한 엔티티, 두 단계
건너 속한 엔티티(`via: "robot.tenant"`), 벽 밖 엔티티.

네 질문 모두 답이 나왔다.

| 물어본 것 | 답 |
| --- | --- |
| 손으로 쓴 것이 proto와 옵션뿐인가 | **그렇다.** `Cell`을 나중에 추가해 봤고, 든 것은 메시지와 옵션 다섯 줄이다. Go는 한 줄도 없다 |
| 도메인을 빼먹으면 생성이 멈추는가 | **멈춘다.** 그리고 무엇을 쓰면 되는지 말한다. 열 가지 거절이 `internal/pdgen`에 테스트로 있다 |
| 벽이 저절로 붙는가 | **붙는다.** `TestAnAddedEntityArrivesWalled`가 그것을 말한다 — 나중에 넣은 엔티티가 이미 벽 뒤에 있다 |
| 도메인이 틀린 ID가 DB 이전에 거절되는가 | **거절된다.** `InvalidArgument`이고, 메시지가 그것이 실제로 무엇이었는지 말한다 |

**손으로 쓴 Go 414줄(전부 테스트), 생성된 Go 9,213줄.** 앱 코드는 0줄이다.

부수적으로 나온 것 둘:

- **상류에 `Minter` 훅이 들어갔다**(`protoc-gen-orm-ent`). `Recorder`·`Scope`와 같은
  자리의 세 번째 훅이고, payday를 모르는 변경이라 포크가 아니라 상류로 갔다.
- **`protoc-gen-orm-service`의 자기 참조 import 버그를 고쳤다.** 한 `.proto`에 엔티티가
  둘 이상이고 하나가 다른 하나를 참조하면 생성된 파일이 자기를 import한다. go-app은
  파일당 엔티티가 하나라 만난 적이 없다.

### CP2 — 런타임 추출이 앱을 실제로 얇게 만드는가 · **완료**

`grpcx`(+`Chain`), `config`, `migrate`, `version`, `slug`, `pdtest`가 payday로 왔고,
`internal/apptest/cmd`가 그 위에서 도는 앱이다.

| | go-app이 들고 있던 것 | 앱이 쓰는 것 |
| --- | --- | --- |
| 설정 기계 | `cmd/config` 1,088줄 | `cmd/config.go` **38줄** — 이름 하나와 임베드 셋 |
| 서빙 체인 | `internal/grpcx` 594줄 | `cmd/serve.go` **106줄** — 스택, 두 서버, 인터셉터 순서 |
| 마이그레이션 | `internal/migrate` 402줄 | 0 |
| 빌드 버전 | 51줄 + 셸 스크립트 | 0 — 툴체인이 이미 찍는다 |
| | **2,135줄** | **144줄** |

**설정만 늘지 않았다.** 앱의 `Config`는 세 필드다 — payday의 조각을 임베드할 뿐이고,
go-app이 갖고 있던 `Evaluate()`(기본값을 채워 넣는 패스)는 사라졌다. 기본값은 값을
묻는 자리에서 답하므로 환경변수보다 먼저 도느냐 나중에 도느냐가 없다.

**배선은 여전히 읽힌다.** `serve.go` 106줄이 스택을 쌓고, 벽 있는 서버와 없는 서버를
따로 만드는 이유를 적고, 인터셉터 순서를 보여준다. `payday.Serve(cfg)` 한 줄이 아니다.

아직 앱에 없는 것은 인증(CP3), CLI, `migrate` 하위 명령, 헬스다 — 위 표는 그만큼
비교 범위가 좁다는 것을 감안해서 읽어야 한다.

#### 나온 것: go-app의 TLS 버그

`TLSConfig.Active()`가 `ClientCAFile`을 세지 않는다.

```go
return c.Enabled || c.CertFile != "" || c.KeyFile != ""
```

그래서 `server.tls.client_ca_file`만 적힌 설정 — `auth.methods: [mtls]`일 때
`Evaluate()`가 **요구하는** 바로 그것 — 은 "활성"이 아니고, `Credentials()`가
`insecure.NewCredentials()`를 돌려준다. 핸드셰이크가 없고, 클라이언트 인증서가 오간
적이 없고, mtls 방법은 프로세스가 사는 동안 "아무도 말하지 않았다"를 답한다.

인증은 닫히는 쪽으로 실패하는데 **기밀성은 열리는 쪽으로 실패한다** — 운영자는 상호
TLS를 적어 놓고 평문을 얻는다. payday에서는 `Active()`가 그것을 세므로
`Credentials()`가 인증서가 없다고 거절하고 서버가 뜨지 않는다.

### CP3 — 스키마 소유가 견딜 만한가 · **완료**

payday가 Tenant/Holder/Audit을 소유하고, 앱은 그것을 **import하지 않고 생성 소스로
복사**한다 — 앱의 메시지·ent 스키마·서버가 한 모듈의 한 타입 집합이어야 해서다.
그래서 `proto/`(옵션, 앱이 import)와 `schema/`(엔티티, 앱이 복사)가 갈렸다.

세 질문 모두 답이 나왔다.

**(1) 필드를 더하는 것은 자연스럽다.** `.ext.proto` 다섯 줄로 `Holder`에
`idp_subject`가 붙고, 병합된 proto → ent 스키마 → 컬럼까지 그대로 간다. 그리고
**조용히 덮는 쪽은 막혔다**: payday의 엔티티를 임베드해서 출하한 것과 생성될 것을
번호·이름·종류로 비교하고, 번호를 다시 선언하면 거절한다.

```
payday.Holder: 4 is payday's "alias" (string) and was redeclared as "handle" (string)
payday keeps 1..7 and 13..15; an app's own go in 8..12 and from 16.
```

**(2) 생성된 `gate`는 읽을 것이 없어서 견딜 만하다** — 그렇게 만들었기 때문이다.
판단은 `payday/gate`와 `payday/audit`에 함수로 있고, 생성되는 것은 그것을 부르는
배선뿐이다. 402줄 중 레이어가 차지하는 것이 200줄가량이고 그 안에 결정은 하나도 없다.
규칙이 무엇인지 찾는 사람은 런타임으로 간다.

문서가 걱정한 모양은 **판단이 생성물로 새어 나가는 것**이었고, 그것은 생성물을 얇게
유지하는 것으로 막힌다. 다만 그 규율은 강제되지 않는다 — CP4에서 `List`를 생성하기
시작하면 다시 시험대에 오른다.

**(3) 업그레이드는 조용하다. 그래서 가드를 만들었다.**

payday의 `Holder`에 필드를 하나 더해 보고 실측했다. 앱이 재생성하면 그 필드가 ent
스키마에 나타나고 — **컴파일되고, 테스트가 통과한다.** 테스트는 방금 만든 데이터베이스를
쓰기 때문이다. 드러나는 곳은 이미 있던 데이터베이스 하나뿐이다.

그래서 `migrate.Current`가 생겼다. 서빙 전에 부르면 파일을 다 돌리지 않은 데이터베이스
위에서는 뜨지 않는다.

```
pd gen                      # 스키마가 움직였다
<app> migrate plan <name>   # 그것이 SQL로 무슨 뜻인지 쓰고, 읽는다
<app> migrate apply         # 돌린다
```

**이것이 §12.1의 답의 절반이다.** 나머지 절반(생성물과 런타임의 버전 불일치)은 아직이다.

**여기까지가 "프로토타입"이다.** 서버가 payday 위에서 돌고, 엔티티를 더하면 강제가
걸리고, 스키마 소유가 실증됐다.

### CP4 — 생성한 `List`가 손으로 쓴 것만큼 좋은가 · **완료**

`list:`/`watch:` 옵션, proto와 구현 생성, 인덱스·키 검사.

| 물어본 것 | 답 |
| --- | --- |
| 생성된 `List`가 손으로 쓴 것과 같은가 | **같다.** narrow → 필터 OR → 커서 → eager load → `Limit(size+1)` → `next` 인코딩까지 go-app의 것과 같은 모양이고, 벽이 한계 **앞에** 들어간다는 것을 테스트가 박아뒀다(남의 행 20개, 내 행 2개, 다섯 개짜리 페이지) |
| 인덱스 검사가 잡는가 | **잡는다.** 경고이고, 붙여넣을 `indexes:` 선언까지 준다. 실제로 선언하니 사라졌다. 거절이 아니라 경고인 것은 작은 테이블이 실재해서다 |
| 선언할 수 없는 필터가 얼마나 나오는가 | **아직 하나도.** go-app의 필터 둘이 모두 동등 비교였고, apptest도 그렇다. 표본이 작다는 것은 감안해야 한다 |

**선을 "필터냐 페이징이냐"가 아니라 "선언할 수 있느냐"로 그었다.** go-app의 PLAN은
"필터는 도메인이라 손으로 쓴다"였는데, 그 저장소가 실제로 가진 필터 둘이 모두 기계적인
것이 답을 바꿨다. 선언할 수 없는 필터는 여전히 RPC를 손으로 쓴다.

**`Watch`는 `List` 위에 선다.** 묘비 없이 값의 부재로 제거를 말하고, 필터 없는 watch를
거절하고, 브로커를 명시하게 한다 — §4.2에서 종이에 적어둔 넷이 그대로 코드가 됐다.

부수적으로 **`Chain` 리팩터링이 연 구멍 둘**이 드러났다. `Limit`과 `Closed`가 "아무것도
설정되지 않음" 가드를 옵션 생성자에만 두고 있었고, 맨 인터셉터를 직접 부르면 nil을
역참조했다. 체인과 배치가 인터셉터를 부르는 방식이 정확히 그것이다.

### CP5 — 브라우저가 실제로 쓸 만한가 · **Go 쪽 완료**

인터셉터 함수화(`grpcx.Chain`, CP2)와 샌드박스의 Go 절반이 섰다.

- `payday`가 **`GOOS=js GOARCH=wasm`으로 그대로 빌드된다.** §7의 4g — 런타임이
  파일 시스템·리스너·네트워크를 전제하지 않는다 — 가 오늘 사실이다.
- `internal/apptest/wasm`이 앱 전체를 페이지 안에서 띄운다. **60.7 MB.** 프로세스와
  다른 줄은 둘뿐이다: 드라이버(`sqlite3-wasm`, 엔진이 자기 워커에서 돈다 — 다른
  드라이버는 wazero 위에서 돌아 여기서는 wasm 안의 wasm이 된다)와 트랜스포트(`jsport`).
- 그것이 가능해진 것은 상류의 한 줄이다: `RegisterServer`가 `*grpc.Server` 대신
  `grpc.ServiceRegistrar`를 받는다. 순수한 넓히기라 기존 호출부는 그대로다.

**남은 것은 페이지다.** npm 쪽 둘(`sqlite3-wasm-go`의 워커, `grpc-dgram`의 TS 절반)을
잇는 일이고 Go가 아니라 프런트 작업이다. 거기서 물릴 두 가지는
`internal/apptest/wasm/README.md`에 적어 뒀다 — COOP/COEP가 없으면
`SharedArrayBuffer`가 아예 없다는 것, 그리고 번들러가 `.wasm`을 URL로 풀고 워커를 띄울
수 있어야 한다는 것.

### CP6 이후

로컬 스토어(Watch 소비), 배치, 로그인, 오류 모양. **서버가 굳은 뒤에 한다** — 전부
서버의 모양을 소비할 뿐 바꾸지 않는다.

### 언제 계획을 다시 쓸 것인가

CP1이나 CP3에서 답이 "아니오"면 문서를 고치는 것이 아니라 **범위를 줄인다.** CP1이
아니면 payday는 프레임워크가 아니라 라이브러리 모음이고, 그것도 쓸모가 있다. CP3이
아니면 Tenant/Holder를 앱에 돌려주고 payday는 `grpcx`+생성기 확장으로 남는다.
둘 다 후퇴가 아니라 **더 작은 것이 맞았다는 답**이다.

## 5. 잃는 것

**읽히지 않는다, 그리고 `gate`/`audit`을 가져오면서 더 심해졌다.** go-app의 가장 큰
자산은 모든 결정이 코드 옆에서 읽힌다는 것인데, 이제 `gate`는 앱 저장소에 *생성된
파일*로 존재한다 — 열어 봐도 판단이 아니라 위임만 보인다. 완화책 셋:

- **런타임에 같은 주석 규율을 지킨다.** 옮기면서 요약하지 않는다. 판단이 사는 곳이
  읽을 수 있어야 한다.
- **생성된 레이어는 얇게 유지한다.** 두꺼워질수록 읽을 것이 생성물로 새어 나간다.
- **배선은 템플릿에 남긴다**([RUNTIME.md](docs/RUNTIME.md) §5). 스택을 쌓는 열 줄은 손으로 쓰인 채로 둔다.

여기서 손해 보는 것이 실재한다는 것은 인정하고 가는 편이 낫다. go-app을 읽고 배울 수
있던 것을 payday 사용자는 payday 저장소를 읽어야 배운다.

**정체성 모델의 뼈대가 고정된다.** 필드는 더할 수 있지만 `id`/`tenant`/`alias`/
`date_erased`는 auth와 벽이 읽으므로 바꿀 수 없다([SCHEMA.md](docs/SCHEMA.md) §2). Holder가 "테넌트에 속한 행위자"가
아닌 앱은 payday를 쓸 이유가 없다 — 이것이 payday를 쓸지 말지를 가르는 조건이다.

**강제는 되돌리기 어렵다.** [RUNTIME.md](docs/RUNTIME.md) §7의 4번은 프레임워크가 앱의 빌드를 깨는 첫 사례가 되고,
그런 항목은 늘어나기만 하고 줄지 않는다. 레이어 순서 강제를 권하지 않은 이유다.

**두 저장소가 번갈아 움직인다.** go-app이 이미 겪은 것 — 생성된 코드는 아직 게시되지
않은 런타임을 쓸 수 없다. `protobuf-orm`을 건드리지 않기로 한 것(§1)과 생성기 변경을
payday-무지하게 유지하는 것([SCHEMA.md](docs/SCHEMA.md))이 이 비용을 최소로 묶어 둔다.
