// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lesomnus/payday/internal/apptest/ent/joint"
	"github.com/lesomnus/payday/internal/apptest/ent/robot"
)

// Joint is the model entity for the Joint schema.
type Joint struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Alias holds the value of the "alias" field.
	Alias string `json:"alias,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JointQuery when eager-loading is set.
	Edges        JointEdges `json:"edges"`
	joint_robot  *uuid.UUID
	selectValues sql.SelectValues
}

// JointEdges holds the relations/edges for other nodes in the graph.
type JointEdges struct {
	// Robot holds the value of the robot edge.
	Robot *Robot `json:"robot,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RobotOrErr returns the Robot value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JointEdges) RobotOrErr() (*Robot, error) {
	if e.Robot != nil {
		return e.Robot, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: robot.Label}
	}
	return nil, &NotLoadedError{edge: "robot"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Joint) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case joint.FieldAlias:
			values[i] = new(sql.NullString)
		case joint.FieldID:
			values[i] = new(uuid.UUID)
		case joint.ForeignKeys[0]: // joint_robot
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Joint fields.
func (_m *Joint) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case joint.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case joint.FieldAlias:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field alias", values[i])
			} else if value.Valid {
				_m.Alias = value.String
			}
		case joint.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field joint_robot", values[i])
			} else if value.Valid {
				_m.joint_robot = new(uuid.UUID)
				*_m.joint_robot = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Joint.
// This includes values selected through modifiers, order, etc.
func (_m *Joint) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRobot queries the "robot" edge of the Joint entity.
func (_m *Joint) QueryRobot() *RobotQuery {
	return NewJointClient(_m.config).QueryRobot(_m)
}

// Update returns a builder for updating this Joint.
// Note that you need to call Joint.Unwrap() before calling this method if this Joint
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Joint) Update() *JointUpdateOne {
	return NewJointClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Joint entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Joint) Unwrap() *Joint {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Joint is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Joint) String() string {
	var builder strings.Builder
	builder.WriteString("Joint(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("alias=")
	builder.WriteString(_m.Alias)
	builder.WriteByte(')')
	return builder.String()
}

// Joints is a parsable slice of Joint.
type Joints []*Joint
