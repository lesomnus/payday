// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"uuid"

	"github.com/lesomnus/payday/internal/apptest/internal/ent/pairing"
	"github.com/lesomnus/payday/internal/apptest/internal/ent/robot"
	"github.com/protobuf-orm/ent"
	"github.com/protobuf-orm/ent/dialect/sql"
)

// Pairing is the model entity for the Pairing schema.
type Pairing struct {
	config `json:"-"`
	// Id of the ent.
	Id uuid.UUID `json:"id,omitempty"`
	// DateCreated holds the value of the "date_created" field.
	DateCreated time.Time `json:"date_created,omitempty"`
	// LeadId holds the value of the "lead_id" field.
	LeadId uuid.UUID `json:"lead_id,omitempty"`
	// FollowId holds the value of the "follow_id" field.
	FollowId uuid.UUID `json:"follow_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PairingQuery when eager-loading is set.
	Edges        PairingEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PairingEdges holds the relations/edges for other nodes in the graph.
type PairingEdges struct {
	// Lead holds the value of the lead edge.
	Lead *Robot `json:"lead,omitempty"`
	// Follow holds the value of the follow edge.
	Follow *Robot `json:"follow,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// LeadOrErr returns the Lead value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PairingEdges) LeadOrErr() (*Robot, error) {
	if e.Lead != nil {
		return e.Lead, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: robot.Label}
	}
	return nil, &NotLoadedError{edge: "lead"}
}

// FollowOrErr returns the Follow value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PairingEdges) FollowOrErr() (*Robot, error) {
	if e.Follow != nil {
		return e.Follow, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: robot.Label}
	}
	return nil, &NotLoadedError{edge: "follow"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Pairing) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pairing.FieldId:
			values[i] = pairing.ValueScanner.Id.ScanValue()
		case pairing.FieldDateCreated:
			values[i] = new(sql.NullTime)
		case pairing.FieldLeadId:
			values[i] = pairing.ValueScanner.LeadId.ScanValue()
		case pairing.FieldFollowId:
			values[i] = pairing.ValueScanner.FollowId.ScanValue()
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Pairing fields.
func (_m *Pairing) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pairing.FieldId:
			if value, err := pairing.ValueScanner.Id.FromValue(values[i]); err != nil {
				return err
			} else {
				_m.Id = value
			}
		case pairing.FieldDateCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date_created", values[i])
			} else if value.Valid {
				_m.DateCreated = value.Time
			}
		case pairing.FieldLeadId:
			if value, err := pairing.ValueScanner.LeadId.FromValue(values[i]); err != nil {
				return err
			} else {
				_m.LeadId = value
			}
		case pairing.FieldFollowId:
			if value, err := pairing.ValueScanner.FollowId.FromValue(values[i]); err != nil {
				return err
			} else {
				_m.FollowId = value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Pairing.
// This includes values selected through modifiers, order, etc.
func (_m *Pairing) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLead queries the "lead" edge of the Pairing entity.
func (_m *Pairing) QueryLead() *RobotQuery {
	return NewPairingClient(_m.config).QueryLead(_m)
}

// QueryFollow queries the "follow" edge of the Pairing entity.
func (_m *Pairing) QueryFollow() *RobotQuery {
	return NewPairingClient(_m.config).QueryFollow(_m)
}

// Update returns a builder for updating this Pairing.
// Note that you need to call Pairing.Unwrap() before calling this method if this Pairing
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Pairing) Update() *PairingUpdateOne {
	return NewPairingClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Pairing entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Pairing) Unwrap() *Pairing {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Pairing is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Pairing) String() string {
	var builder strings.Builder
	builder.WriteString("Pairing(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.Id))
	builder.WriteString("date_created=")
	builder.WriteString(_m.DateCreated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("lead_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LeadId))
	builder.WriteString(", ")
	builder.WriteString("follow_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.FollowId))
	builder.WriteByte(')')
	return builder.String()
}

// PairingList is a parsable slice of Pairing.
type PairingList []*Pairing
