// Code generated by protoc-gen-orm-ent. DO NOT EDIT.
// source: shared/thing.proto

package bare

import (
	context "context"
	errors "errors"
	uuid "github.com/google/uuid"
	apptest "github.com/lesomnus/payday/internal/apptest"
	ent "github.com/lesomnus/payday/internal/apptest/internal/ent"
	predicate "github.com/lesomnus/payday/internal/apptest/internal/ent/predicate"
	thing "github.com/lesomnus/payday/internal/apptest/internal/ent/thing"
	patchpb "github.com/lesomnus/protobuf-patch/patchpb"
	sqlgraph "github.com/protobuf-orm/ent/dialect/sql/sqlgraph"
	ormpatch "github.com/protobuf-orm/protobuf-orm/ormpatch"
	entpatch "github.com/protobuf-orm/protoc-gen-orm-ent/runtime/entpatch"
	enttx "github.com/protobuf-orm/protoc-gen-orm-ent/runtime/enttx"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

type ThingServiceServer struct {
	Store

	apptest.UnimplementedThingServiceServer
}

// NewThingServiceServer answers with a server that runs its queries with `db`.
//
// It takes the options of [Server] so that what is built here can be told
// where to report its writes and what it may see. Built without them, it
// reports nowhere and sees everything.
func NewThingServiceServer(db *ent.Client, opts ...Option) apptest.ThingServiceServer {
	s := Server{Store: Store{Db: db}}
	for _, opt := range opts {
		opt(&s)
	}
	return ThingServiceServer{Store: s.Store}
}

// ThingNarrow answers with `p` and everything else that narrows a
// read of a Thing: the rows that have not been erased, and whatever
// `scope` says of those.
//
// Every read this package makes goes through it, and a read written by
// hand should too -- a List is the one read nothing generates, and so the
// one that would otherwise answer with rows nobody should be given.
func ThingNarrow(ctx context.Context, scope Scope, p predicate.Thing) (predicate.Thing, error) {
	ps := make([]predicate.Thing, 0, 3)

	// A row that was erased is not a row a read answers with.
	ps = append(ps, thing.DateErasedIsNil())
	if p != nil {
		ps = append(ps, p)
	}
	if scope != nil {
		q, err := scope.ThingScope(ctx)
		if err != nil {
			return nil, err
		}
		if q != nil {
			ps = append(ps, q)
		}
	}

	switch len(ps) {
	case 0:
		return nil, nil
	case 1:
		return ps[0], nil
	default:
		return thing.And(ps...), nil
	}
}

// narrow is [ThingNarrow] with this server's own scope.
func (s ThingServiceServer) narrow(ctx context.Context, p predicate.Thing) (predicate.Thing, error) {
	return ThingNarrow(ctx, s.Scope, p)
}

func (s ThingServiceServer) Add(ctx context.Context, req *apptest.ThingAddRequest) (*apptest.Thing, error) {
	tx, err := enttx.Join[*ent.Client, *ent.Tx](ctx, s.Db, s.Rec != nil)
	if err != nil {
		return nil, err
	}
	defer tx.Close()

	st := s
	st.Db = tx.Db

	q := st.Db.Thing.Create()
	var k uuid.UUID
	if req.HasId() {
		if v, err := uuid.FromBytes(req.GetId()); err != nil {
			return nil, status.Errorf(codes.InvalidArgument, "id: %s", err)
		} else {
			k = v
		}
	}
	if v, err := mint(ctx, s.Mint, "shared.Thing", k, req.HasId()); err != nil {
		return nil, err
	} else {
		q.SetId(v)
	}
	q.SetAlias(req.GetAlias())
	if req.HasDateCreated() {
		q.SetDateCreated(req.GetDateCreated().AsTime())
	} else {
		q.SetDateCreated(st.now())
	}

	u, err := q.Save(ctx)
	if err != nil {
		if err, ok := err.(*ent.ConstraintError); ok {
			if sqlgraph.IsUniqueConstraintError(err) {
				return nil, status.Error(codes.AlreadyExists, "Thing already exists")
			}
			if sqlgraph.IsForeignKeyConstraintError(err) {
				return nil, status.Error(codes.NotFound, "Thing: referenced entity not found")
			}
		}
		return nil, err
	}

	if err := record(ctx, s.Rec, st.Db, Change{
		By:  apptest.ThingService_Add_FullMethodName,
		Key: u.Id,
	}); err != nil {
		return nil, err
	}
	if err := tx.Commit(); err != nil {
		return nil, err
	}

	return u.Proto(), nil
}

func (s ThingServiceServer) Get(ctx context.Context, req *apptest.ThingGetRequest) (*apptest.Thing, error) {
	p, err := ThingPick(req.GetRef())
	if err != nil {
		return nil, err
	}
	p, err = s.narrow(ctx, p)
	if err != nil {
		return nil, err
	}

	q := s.Db.Thing.Query().Where(p)
	ThingSelectInit(q, req.GetSelect())

	v, err := q.Only(ctx)
	if err != nil {
		if ent.IsNotFound(err) {
			return nil, status.Error(codes.NotFound, "Thing not found")
		}
		return nil, err
	}
	return v.Proto(), nil
}

func selectThingKey(q *ent.ThingQuery) {
	q.Select(thing.FieldId)
}

func ThingSelectedFields(m *apptest.ThingSelect) []string {
	if m.GetAll() {
		return thing.Columns
	}

	vs := make([]string, 0, len(thing.Columns))
	{
		vs = append(vs, thing.FieldId)
	}
	if m.GetAlias() {
		vs = append(vs, thing.FieldAlias)
	}
	if m.GetDateErased() {
		vs = append(vs, thing.FieldDateErased)
	}
	if m.GetDateCreated() {
		vs = append(vs, thing.FieldDateCreated)
	}

	return vs
}

func ThingSelect(q *ent.ThingQuery, m *apptest.ThingSelect) {
	if !m.GetAll() {
		fields := ThingSelectedFields(m)
		q.Select(fields...)
	}
}

func ThingSelectInit(q *ent.ThingQuery, m *apptest.ThingSelect) {
	if m != nil {
		ThingSelect(q, m)
	} else {
	}
}

func (s ThingServiceServer) Patch(ctx context.Context, req *apptest.ThingPatchRequest) (*apptest.Thing, error) {
	doc, err := ormpatch.FromPatchRequest(thingOrmEntity, req.ProtoReflect(), nil)
	if err != nil {
		if _, ok := status.FromError(err); ok {
			return nil, err
		}
		if errors.Is(err, ormpatch.ErrRequestLayout) {
			return nil, status.Errorf(codes.Internal, "%s", err)
		}
		if errors.Is(err, ormpatch.ErrUnsupported) {
			return nil, status.Errorf(codes.Unimplemented, "%s", err)
		}
		return nil, status.Errorf(codes.InvalidArgument, "%s", err)
	}

	return s.apply(ctx, req.GetRef(), doc, apptest.ThingService_Patch_FullMethodName)
}

func ThingGetKey(ctx context.Context, db *ent.Client, ref *apptest.ThingRef) (uuid.UUID, error) {
	var z uuid.UUID
	if ref.HasId() {
		if v, err := uuid.FromBytes(ref.GetId()); err != nil {
			return z, status.Errorf(codes.InvalidArgument, "id: %s", err)
		} else {
			return v, nil
		}
	}

	p, err := ThingPick(ref)
	if err != nil {
		return z, err
	}

	v, err := db.Thing.Query().Where(p).OnlyId(ctx)
	if err != nil {
		if ent.IsNotFound(err) {
			return z, status.Error(codes.NotFound, "Thing not found")
		}
		return z, err
	}

	return v, nil
}

var thingOrmEntity = ormpatch.MustEntityOf(apptest.File_shared_thing_proto, "Thing")

var thingPatchColumns = entpatch.Columns{
	1: thing.FieldId, 4: thing.FieldAlias, 14: thing.FieldDateErased, 15: thing.FieldDateCreated}

func (s ThingServiceServer) Apply(ctx context.Context, req *apptest.ThingApplyRequest) (*apptest.Thing, error) {
	if !req.HasPatch() {
		return nil, status.Errorf(codes.InvalidArgument, "%s", ormpatch.ErrNoPatch)
	}
	return s.apply(ctx, req.GetRef(), req.GetPatch(), apptest.ThingService_Apply_FullMethodName)
}

func (s ThingServiceServer) apply(ctx context.Context, ref *apptest.ThingRef, doc *patchpb.Patch, by string) (*apptest.Thing, error) {
	plan := &ormpatch.Plan{Entity: thingOrmEntity}
	if doc != nil {
		v, err := ormpatch.Compile(thingOrmEntity, doc)
		if err != nil {
			if errors.Is(err, ormpatch.ErrUnsupported) {
				return nil, status.Errorf(codes.Unimplemented, "%s", err)
			}
			return nil, status.Errorf(codes.InvalidArgument, "%s", err)
		}
		plan = v
	}

	pred, mod, err := entpatch.Build(plan, thingPatchColumns, s.Db.Dialect())
	if err != nil {
		if errors.Is(err, entpatch.ErrValue) {
			return nil, status.Errorf(codes.InvalidArgument, "%s", err)
		}
		return nil, status.Errorf(codes.Internal, "%s", err)
	}

	tx, err := enttx.Join[*ent.Client, *ent.Tx](ctx, s.Db, true)
	if err != nil {
		return nil, err
	}
	defer tx.Close()

	st := s
	st.Db = tx.Db

	k, err := ThingGetKey(ctx, st.Db, ref)
	if err != nil {
		return nil, err
	}
	at := &apptest.ThingRef{}
	at.SetId(k[:])
	p, err := s.narrow(ctx, thing.IdEQ(k))
	if err != nil {
		return nil, err
	}

	if mod == nil {
		q := st.Db.Thing.Query().Where(p)
		if pred != nil {
			q.Where(predicate.Thing(pred))
		}
		if ok, err := q.Exist(ctx); err != nil {
			return nil, err
		} else if !ok {
			return nil, func() error {
				if ok, err := st.Db.Thing.Query().Where(p).Exist(ctx); err != nil {
					return err
				} else if !ok {
					return status.Error(codes.NotFound, "Thing not found")
				}
				return status.Error(codes.FailedPrecondition, "a test in the patch did not hold")
			}()
		}
	} else {
		q := st.Db.Thing.Update().Where(p)
		if pred != nil {
			q.Where(predicate.Thing(pred))
		}
		q.Modify(mod)
		if n, err := q.Save(ctx); err != nil {
			if err, ok := err.(*ent.ConstraintError); ok {
				if sqlgraph.IsUniqueConstraintError(err) {
					return nil, status.Error(codes.AlreadyExists, "Thing already exists")
				}
				if sqlgraph.IsForeignKeyConstraintError(err) {
					return nil, status.Error(codes.NotFound, "Thing: referenced entity not found")
				}
			}
			return nil, err
		} else if n == 0 {
			return nil, func() error {
				if ok, err := st.Db.Thing.Query().Where(p).Exist(ctx); err != nil {
					return err
				} else if !ok {
					return status.Error(codes.NotFound, "Thing not found")
				}
				return status.Error(codes.FailedPrecondition, "a test in the patch did not hold")
			}()
		}
	}

	if mod != nil {
		if err := record(ctx, s.Rec, st.Db, Change{
			By:    by,
			Key:   k,
			Patch: doc,
		}); err != nil {
			return nil, err
		}
	}

	out, err := st.Get(ctx, at.Pick())
	if err != nil {
		return nil, err
	}
	if err := tx.Commit(); err != nil {
		return nil, err
	}
	return out, nil
}

func (s ThingServiceServer) Erase(ctx context.Context, req *apptest.ThingRef) (*apptest.ThingEraseResponse, error) {
	p, err := ThingPick(req)
	if err != nil {
		return nil, err
	}
	p, err = s.narrow(ctx, p)
	if err != nil {
		return nil, err
	}

	tx, err := enttx.Join[*ent.Client, *ent.Tx](ctx, s.Db, s.Rec != nil)
	if err != nil {
		return nil, err
	}
	defer tx.Close()

	st := s
	st.Db = tx.Db

	var k any
	if s.Rec != nil {
		v, err := st.Db.Thing.Query().Where(p).OnlyId(ctx)
		if err != nil {
			if ent.IsNotFound(err) {
				return &apptest.ThingEraseResponse{}, nil
			}
			return nil, err
		}

		k = v
		p = thing.And(p, thing.IdEQ(v))
	}

	u := st.Db.Thing.Update().Where(p)
	u.SetDateErased(st.now())
	n, err := u.Save(ctx)
	if err != nil {
		return nil, err
	}
	if n > 0 {
		if err := record(ctx, s.Rec, st.Db, Change{
			By:  apptest.ThingService_Erase_FullMethodName,
			Key: k,
		}); err != nil {
			return nil, err
		}
	}
	if err := tx.Commit(); err != nil {
		return nil, err
	}
	res := &apptest.ThingEraseResponse{}
	res.SetErased(n > 0)

	return res, nil
}

// ThingPick answers with the predicate this reference selects on,
// among the rows that are still here.
//
// Erasure is part of the reference and not only part of a read's scope,
// because a reference to a Thing is composed into the reference of
// whatever names one: an index over an edge asks this for a predicate and
// puts it inside `HasThingWith`, where no narrowing of a Thing
// is ever applied. A child of an erased row would otherwise be readable by
// naming its parent.
func ThingPick(req *apptest.ThingRef) (predicate.Thing, error) {
	p, err := pickThing(req)
	if err != nil {
		return nil, err
	}

	return thing.And(thing.DateErasedIsNil(), p), nil
}

func pickThing(req *apptest.ThingRef) (predicate.Thing, error) {
	switch req.WhichKey() {
	case apptest.ThingRef_Id_case:
		if v, err := uuid.FromBytes(req.GetId()); err != nil {
			return nil, status.Errorf(codes.InvalidArgument, "id: %s", err)
		} else {
			return thing.IdEQ(v), nil
		}
	case apptest.ThingRef_Key_not_set_case:
		return nil, status.Errorf(codes.InvalidArgument, "key not set: Thing")
	default:
		return nil, status.Errorf(codes.Unimplemented, "unknown type of key: %s", req.WhichKey())
	}
}
